CREATE TABLE [dbo].[ESFile] (
    [id]                    INT            IDENTITY (1, 1) NOT NULL,
    [name]                  VARCHAR (1024) NULL,
    [ESService]             INT            NULL,
    [ESUptimeHistory]       INT            NULL,
    [ESDiskspaceLargeFiles] INT            NULL,
    CONSTRAINT [PK_ESFile] PRIMARY KEY CLUSTERED ([id] ASC)
);

