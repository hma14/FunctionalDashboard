CREATE TABLE [dbo].[ESEventlogData] (
    [id]         INT             IDENTITY (1, 1) NOT NULL,
    [eventdata]  VARBINARY (MAX) NULL,
    [datalength] INT             NULL,
    CONSTRAINT [PK_ESEventlogData] PRIMARY KEY CLUSTERED ([id] ASC)
);

