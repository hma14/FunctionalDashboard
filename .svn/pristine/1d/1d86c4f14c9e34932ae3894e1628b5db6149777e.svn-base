CREATE TABLE [dbo].[ESAction] (
    [id]                     INT           IDENTITY (1, 1) NOT NULL,
    [name]                   VARCHAR (128) NULL,
    [ESActionTriggerHistory] INT           NULL,
    CONSTRAINT [PK_ESAction] PRIMARY KEY CLUSTERED ([id] ASC)
);

