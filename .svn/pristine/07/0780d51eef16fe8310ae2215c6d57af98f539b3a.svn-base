CREATE TABLE [dbo].[ESEventlogAccountGroup] (
    [id]                      INT           IDENTITY (1, 1) NOT NULL,
    [name]                    VARCHAR (128) NULL,
    [ESTrackingAccountGroups] INT           NULL,
    CONSTRAINT [PK_ESEventlogAccountGroup] PRIMARY KEY CLUSTERED ([id] ASC)
);

