CREATE TABLE [dbo].[ESPerformanceName] (
    [id]            INT           IDENTITY (1, 1) NOT NULL,
    [name]          VARCHAR (255) NULL,
    [ESPerformance] INT           NULL,
    CONSTRAINT [PK_ESPerformanceName] PRIMARY KEY CLUSTERED ([id] ASC)
);

