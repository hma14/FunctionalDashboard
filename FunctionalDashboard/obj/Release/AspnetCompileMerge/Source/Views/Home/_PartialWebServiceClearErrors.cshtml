@model FunctionalDashboard.ViewModels.WebServiceOperationStatus
@using FunctionalDashboard.Models

@{
    Layout = null;
}


@{
    if (Model != null)
    {
        @Html.Raw(String.Format("Error ({0:#,###0} ({1:#,###0}) )", --Model.TotalErrors, ++Model.ClearedErrors))
    }
      
}
